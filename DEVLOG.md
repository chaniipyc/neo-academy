# NEO ACADEMY 개발 일지

원생관리 시스템을 만들어온 흐름과 그 과정에서의 생각, 문제, 결정들을 정리한 기록입니다. 코드는 Git에 버전별로 남아있지만, "왜 그렇게 만들었는지"는 코드만 봐서는 알 수 없어서 별도로 남깁니다.

---

## 1장 — 뼈대 세우기 (2026-08-18 ~ 19)

정적 웹사이트(HTML/CSS/JS, 백엔드 없음)로 시작. 로그인, 학생 정보 등록/수정/삭제, 홈/관리자 페이지의 기본 골격을 먼저 세웠다.

- 첫 디자인 리뷰에서 "사이트가 전체적으로 투박하다"는 피드백 → 그림자·간격·색감을 다듬는 1차 리디자인 진행. 홈페이지 카테고리 아래 여백을 늘리고, 최하단 푸터의 진한 보라색 톤을 줄임.
- 실 데이터 없이 기능을 테스트하기 위해 **더미 학생 100명**을 생성. 조건: 수학A/B/C, 국어A/B/C, 영어A/B/C 총 9개 강좌 중 학생마다 1~3개를 배정하되 같은 과목 중복 수강은 없도록 제약을 걸어 생성.
- 검색 기능은 처음엔 이름·학교·학년·현재수강강좌를 한꺼번에 검색했는데, "전화번호 검색인데 010이 다 걸린다"는 문제 발견 → 검색 대상 필드를 좁히고, 이후 **체크박스로 검색 범위를 직접 선택**하는 방식으로 재설계.
- 학생 상세 페이지에 뒤로가기 버튼 추가.

---

## 2장 — 첫 배포, 그리고 첫 보안 사고 (2026-08-20)

GitHub Pages로 실제 배포하기로 결정 (무료, `chaniipyc.github.io/neo-academy`).

배포 직전 코드를 다시 훑어보다가 **관리자 로그인 비밀번호가 클라이언트 JS 코드에 평문으로 박혀 있는 것**을 발견했다. 저장소가 Public이라 소스를 보면 누구나 비밀번호를 알 수 있는 상태였음. 배포를 잠시 멈추고 비밀번호를 새 값으로 교체한 뒤에 배포를 진행했다.

이때부터 **"배포는 반드시 먼저 확인받고 진행한다"**는 원칙이 세워졌고, 이후 모든 배포 전에 확인 절차를 거치게 됨.

---

## 3장 — 메뉴 단순화와 반별관리의 탄생 (2026-08-20 ~ 21)

- 마우스를 올려야 나오는 드롭다운 서브메뉴를 없애고, 학생 정보 / 상담 / 반별 관리 **3개 큰 카테고리를 클릭 한 번에 바로 이동**하도록 단순화.
- **반별 관리** 기능을 처음부터 설계: 반 생성 → 학생 배정 → 완강처리 → History(완강 기록) → 되돌리기. 더미 데이터에 이미 있던 9개 강좌를 기준으로 반을 자동 생성하도록 함.
- 개발 도중 같은 코드를 고쳐도 브라우저에 반영이 안 되는 문제를 반복적으로 겪음. 원인을 추적한 끝에 **브라우저의 공격적인 캐싱** 때문임을 확인 → 캐시를 막는 전용 개발 서버(`dev-server.py`)를 직접 만들고, 스크립트 태그에 버전 쿼리(`?v=1`, `?v=2`, …)를 붙여 근본적으로 해결.
- 학생 목록/상세 페이지에서 "현재 수강 수업" 이름을 누르면 해당 반의 **반 상세 페이지**로, 반 안에서 학생 이름을 누르면 다시 학생 상세로 넘어가도록 서로 연결. 뒤로가기는 항상 고정된 페이지가 아니라 **직전에 있던 페이지로** 돌아가도록 브라우저 히스토리 기반으로 구현.

---

## 4장 — 출결·시험 관리: 세 번의 재설계 (2026-08-21)

이 기능이 가장 여러 번 뒤집힌 부분이다.

1. **1차 시도**: 출석/결석/지각/조퇴를 클릭하면 순서대로 바뀌는 방식으로 만듦.
2. **2차 시도**: "출결 사유가 그 네 가지 말고도 많다"는 피드백 → 클릭식을 버리고 **자유 텍스트 입력 + 엑셀처럼 빈 칸에 날짜를 직접 써서 새 열을 만드는 방식**으로 전면 재설계. 시험도 "시험 추가" 버튼 대신 표 오른쪽 빈 칸에 날짜·만점을 바로 입력하는 방식으로 바꿈 (시험이 꼭 당일에만 생기는 게 아니라서).
3. **3차 다듬기**: 점수를 0~만점 범위로 제한, 이름 열은 가로 스크롤에도 왼쪽에 고정, 헤더 행은 세로 스크롤에도 위에 고정, 새 열 추가 칸은 오른쪽 끝에 고정 — 학생·시험이 아무리 늘어나도 쓰기 편하도록 스프레드시트처럼 다듬음.

이 과정에서 **버그 하나를 실제로 잡아냈다**: Enter를 눌러 다음 학생 칸으로 넘어가는 기능이 안 된다는 신고를 받고 재현해보니, 포커스를 옮긴 직후 발생하는 저장 로직이 표 전체를 다시 그리면서 방금 옮긴 포커스를 즉시 지워버리는 **경쟁 상태(race condition)** 였음. 저장 → 다시 그리기 → 포커스 이동의 순서를 코드에서 직접 확정하는 방식으로 고침.

또한 "반을 완강 처리해도 출결·시험 기록은 사라지면 안 된다"는 요구사항을 충족하도록, 기록은 반 자체에 저장되고 완강 여부와 무관하게 History에서 계속 열람 가능하게 설계함. (나중에 "학생을 다른 반으로 옮겼다가 되돌리면 기록이 복구되냐"는 질문에 실제로 재현 테스트를 해봤고, 이미 의도대로 복구됨을 확인함.)

---

## 5장 — 마무리 다듬기와 현실 점검 (2026-08-22)

- "버튼 모양·크기가 별로"라는 피드백 → 색은 그대로 두고, 버튼마다 제각각이던 둥근 정도·패딩·그림자를 하나의 기준으로 통일.
- 그 여파로 "뒤로" 버튼도 다른 버튼들과 똑같이 박스형이 되어 오히려 어색해짐 → 가벼운 텍스트 링크 형태로 다시 조정하고, 넓은 표를 쓰는 페이지에서 살짝 어긋나 있던 정렬도 함께 맞춤.
- **실사용 전 보안·개인정보 점검**을 진행. 결론: 지금 구조(정적 사이트 + 브라우저 저장소)로는 (1) 선생님이 여러 명이면 데이터가 공유되지 않고, (2) 관리자 비밀번호가 공개 코드에 노출돼 있고, (3) 데이터 백업이 없어 유실 위험이 크고, (4) 개인정보보호법이 요구하는 기술적 보호조치 수준에 못 미친다는 점을 확인. **실제 학생·학부모 개인정보를 다루려면 진짜 데이터베이스와 서버 기반 인증으로의 이전이 선행되어야 한다**는 결론을 내림 (다음 단계로 남겨둠).

---

## 지금까지 저장된 버전 (Git 커밋 기록)

| 시점 | 날짜 | 내용 |
|---|---|---|
| 1 | 2026-08-20 | 최초 버전 — 원생관리 시스템 기본 틀 |
| 2 | 2026-08-21 새벽 | 메뉴 단순화, 반별 관리 추가 |
| 3 | 2026-08-21 오후 | 반 상세 페이지, 출결·시험 관리 추가 |
| 4 | 2026-08-22 | 버튼 디자인 통일, 뒤로가기 정렬 수정 |

배포 사이트: https://chaniipyc.github.io/neo-academy/
저장소: https://github.com/chaniipyc/neo-academy

---

## 앞으로 남은 것

- **실사용 전 필수**: 데이터베이스 + 서버 기반 인증으로 이전 (Firebase/Supabase 검토 중)
- 상담 기능 — 아직 내용 미확정, 자리만 잡아둔 상태
- 이후 계속 추가될 기능들
